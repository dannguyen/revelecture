// to be killed

jest.disableAutomock();
import Anecdote from '../src/lib/Anecdote';

describe('Anecdote with children', () => {

  xit('should resolve children path relative to the parent dir', () => {

  });


  xit('should attach children to parent as an array', () => {

  });

  xit('children array is empty by default', () => {

  });

  xit('child anecdote should have pointer to parent (filename?)', () => {

  });

  xit('child anecdote should throw error if it has its own children...', () => {

  });

  xit('should render title attribute as h3 for child anecdotes', () => {

  });


})
