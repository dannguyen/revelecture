jest.disableAutomock();
import Anecdote from '../src/lib/Anecdote';

describe('Anecdote background settings', () => {

  describe(':background attribute is Objectively an Object', () => {
    xit('should be an object', () => {

    });

    xit('should throw an error if background-[whatever] is set', () => {

    });
  })


  describe(':backgrounds: attribute', () => {
    xit(':backgrounds is an Object belonging to Anecdote', () => {

    });

    xit('set backgrounds.color attribute', () => {

    });

    xit('set backgrounds.image attribute', () => {

    });
  })

  describe('how :backgrounds is rendered as a slide', () => {
    xit('should contain data-background-image [etc]', () => {

    })
  })



});
